spring.application.name=chess_app
spring.data.mongodb.uri=mongodb://localhost:27017/Art_Of_War

# Firebase Configuration
firebase.project-id=${FIREBASE_PROJECT_ID:shatranj-7de57}
firebase.service-account-path=${FIREBASE_SERVICE_ACCOUNT_PATH:}

# CORS Configuration (remove duplicate)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:3002}

# Debug
app.debug.enabled=${DEBUG_ENABLED:false}

# Server (remove duplicate)
server.port=${SERVER_PORT:8080}
