spring.application.name=chess_app
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://YGuptaji:Gg6914%21%40%23%24@cluster0.tq3avkg.mongodb.net/chess_db?retryWrites=true&w=majority&appName=Cluster0}

# Firebase Configuration
firebase.project-id=${FIREBASE_PROJECT_ID:shatranj-7de57}
firebase.service-account-path=${FIREBASE_SERVICE_ACCOUNT_PATH:firebase_service.json}

# CORS Configuration
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3001,http://localhost:3000,https://shatranj-art-of-war.vercel.app}

# Debug
app.debug.enabled=${DEBUG_ENABLED:false}

# Server
server.port=${PORT:8080}


# MongoDB Logging (optional, helps with debugging)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG


# Avoid auto-config that causes crash
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

# Redis Configuration
spring.redis.host=${REDIS_HOST:redis-16340.c330.asia-south1-1.gce.redns.redis-cloud.com}
spring.redis.port=${REDIS_PORT:16340}
spring.redis.password=${REDIS_PASSWORD:TBKdyeSpaY6ILKe0nVwGBjNZVjYTzIBf}

auth.secure=${AUTH_SECURE:false}
auth.cross.site=${AUTH_CROSS_SITE:true}

